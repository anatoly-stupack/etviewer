========================================================================
    STATIC LIBRARY : GoogleTestLib Project Overview
========================================================================

This static library project was created to compile the google test lib into the platforms this
project supports.

The .lib files that are generated are saved into their corrosponding Release/Debug/Release_XP/Debug_XP 
folders that the ETViewer project uses.
